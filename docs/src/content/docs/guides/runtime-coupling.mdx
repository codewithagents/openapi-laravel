---
title: Runtime coupling of generated code (DRAFT proposal)
description: Generated code currently imports four support classes from the generator package, making it a runtime dependency. The 1.0.0 question, keep that dependency or make generation self-contained, with options, tradeoffs, and a recommendation. A draft proposal pending a maintainer decision.
---

import { Aside } from '@astrojs/starlight/components'

<Aside type="caution" title="DRAFT proposal, not yet adopted">
  This page is a **draft proposal** written for the 1.0.0 readiness pass ([issue #40](https://github.com/codewithagents/openapi-laravel/issues/40)). It analyzes a decision the project has not yet made and does **not** describe shipped behavior beyond the "current state" section. The decision is the maintainer's; it is flagged at the bottom. Nothing here is implemented.
</Aside>

## Current state: generated code depends on the generator at runtime

The generated Data classes do not just stand alone in your repo. Some of them **import support classes from the generator's own namespace**:

- `CodeWithAgents\OpenApiLaravel\Support\HostnameRule` (a `format: hostname` property)
- `CodeWithAgents\OpenApiLaravel\Support\MultipleOfRule` (a `multipleOf` property)
- `CodeWithAgents\OpenApiLaravel\Support\Rfc3339DateTimeRule` (a `format: date-time` property)
- `CodeWithAgents\OpenApiLaravel\Support\MapObjectTransformer` (every `additionalProperties` map property, via `#[WithTransformer(...)]`)

These imports are part of the **committed generated output** (they are pinned by the conformance golden test), not an accident. They exist because Laravel has no native rule for `multipleOf`, no strict RFC3339 `date-time` rule, no hostname rule, and no way to force an empty map to serialize as `{}` rather than `[]`. The generator supplies those four small classes and the generated code references them.

The consequence is the headline of this proposal: **`codewithagents/openapi-laravel` is a runtime dependency of every consuming application, not just a dev-time tool.** If you generate a class that uses `multipleOf` or a date-time format, your production app loads `MultipleOfRule` from the generator package at request time.

### Why that sits in tension with the philosophy

The project's stated philosophy is [you own the output](/philosophy): readable PHP in your repo that keeps working even if you stop using the generator. The runtime coupling dents that in two ways:

- **It is not fully owned.** Four classes that your generated `rules()` depend on live in `vendor/`, outside the code you committed and review in diffs.
- **A `composer update` can change runtime behavior under committed code.** Because the support classes are versioned with the generator, upgrading the generator can change how your already-generated, already-committed classes validate and serialize, without you regenerating anything. That is the exact silent-change surface the [versioning proposal](/guides/versioning-policy) is trying to pin down.

`spatie/laravel-data` itself is a genuine runtime dependency under **every** option below, that is unavoidable and expected, the generated classes *are* laravel-data classes. The open question is only about the four `openapi-laravel`-owned support classes.

## The 1.0.0 question

This must settle **before** 1.0.0, because the import lines are part of the frozen output format. Moving the namespace later (for example from `CodeWithAgents\OpenApiLaravel\Support\...` to `App\Data\Support\...`) is a breaking output change, exactly what the 1.0.0 freeze is meant to prevent. The decision also compounds: the discriminator-aware cast planned for 1.0.0 ([#38](https://github.com/codewithagents/openapi-laravel/issues/38)) will likely add another runtime class, so whatever rule we set now applies to a growing set.

## Option A: keep the runtime dependency (status quo)

Generated code keeps importing the support classes from the generator package. `codewithagents/openapi-laravel` stays a `require` (not `require-dev`) for consumers.

**Pros**

- **Zero duplication.** One canonical implementation of each rule, maintained and tested in one place.
- **Bug fixes flow to consumers without regeneration.** If `Rfc3339DateTimeRule` has a parsing bug, a `composer update` fixes it everywhere.
- **Simplest generator.** No extra files to emit, no namespace rewriting, no per-project copies to keep in sync.
- **Already shipped and proven** (the e2e demo runs exactly this).

**Cons**

- **Generated code is not self-contained.** "Stop using the generator and your code still works" is false: remove the package and the classes that import `Support\...` break.
- **Silent runtime-behavior changes on upgrade**, as above. This is the strongest argument against A.
- **The generator is a heavier dependency than it needs to be.** Consumers pull the whole generator (parser, emitter, cebe, symfony/yaml) into production just to get four small runtime classes.

## Option B: inline the support classes into the consumer's output

The generator emits the four support classes into the consumer's own namespace alongside the Data classes (for example `App\Data\Support\MultipleOfRule`), and the generated code imports from there. They are owned, committed, and drift-checked like everything else.

**Pros**

- **Fully owned output.** The philosophy holds literally: every class the generated code touches is in your repo and survives removing the generator.
- **No runtime dependency on the generator.** It becomes a pure dev-time / `require-dev` tool.
- **No silent runtime changes.** A rule only changes when you regenerate and review the diff, the same contract as the rest of the output.

**Cons**

- **Duplication across projects.** Every consuming project carries its own copy of the four classes. A fix to a rule reaches them only on regeneration (which is consistent with the owned-output model, but means no automatic propagation).
- **More generated files** (four extra per project) and a small amount of generator complexity (emitting them, namespacing them, drift-checking them).
- **The drift check now covers the support classes too**, which is arguably more correct but is more surface.

## Option C: a tiny frozen runtime package

Split the four support classes into a separate, minimal, semver-frozen package (for example `codewithagents/openapi-laravel-runtime`) that changes essentially never. The generator depends on it; generated code imports from it; consumers `require` only the tiny runtime, not the whole generator.

**Pros**

- **Runtime footprint shrinks** to four classes plus laravel-data, no parser or emitter in production.
- **One canonical implementation** (no duplication), like Option A.
- **A frozen package has a clean, narrow versioning story:** it changes almost never, so the silent-upgrade risk is small and explicitly bounded.

**Cons**

- **Still a runtime dependency**, so the "owned output, survives removing the generator" promise is only partly satisfied (you still need the runtime package).
- **A second package to publish, version, and maintain**, plus the coordination of keeping the generator's emitted imports in lockstep with the runtime package's namespace.
- **Splitting a package for four small classes** can feel heavier than the problem warrants.

## How the options compare

| | A: keep dependency | B: inline into output | C: frozen runtime package |
| --- | --- | --- | --- |
| Generated code self-contained | No | **Yes** | No |
| Runtime dep on generator | Yes (full) | **None** | Tiny runtime only |
| Duplication across projects | None | Per-project copies | None |
| Silent runtime change on `composer update` | **Possible** | None | Rare (frozen) |
| `spatie/laravel-data` still required | Yes | Yes | Yes |
| Generator/maintenance complexity | Lowest | Medium (emit + drift-check) | Medium (second package) |
| Aligns with "you own the output" | Partly | **Fully** | Partly |

## Recommendation

**Option B (inline into the consumer's output).** It is the only option that makes the [you own the output](/philosophy) promise literally true, it removes the silent-runtime-change surface entirely (rule behavior changes only on a reviewed regeneration, like everything else), and it makes the generator a clean dev-time tool. The cost, four extra generated files and a little emitter work, is small and exactly the kind of owned, drift-checked output the project already produces. The duplication tradeoff is consistent with the model: you already re-own every Data class per project, the support classes are no different.

Option C is the reasonable fallback if duplication is judged unacceptable: it keeps one canonical implementation while shrinking the runtime footprint and bounding the upgrade risk to a near-frozen package. Option A is the lowest-effort path but is the weakest fit for the stated philosophy and carries the silent-upgrade surface that the versioning proposal is otherwise trying to close.

## Open decision for the maintainer

<Aside type="caution" title="Decision needed before 1.0.0">
  **This is Benjamin's call, and it must be made before 1.0.0** because the import namespace is part of the frozen output format.

  - **The choice:** A (keep the runtime dependency), B (inline the four support classes into the consumer's namespace, recommended), or C (a tiny frozen runtime package).
  - **It compounds with [#38](https://github.com/codewithagents/openapi-laravel/issues/38):** the discriminator-aware cast will likely add a fifth runtime class, so the rule chosen now governs a growing set.
  - **It feeds [#41 (versioning policy)](/guides/versioning-policy):** the breaking-change wording for the support classes depends on where they live. Decide #40 first, then finalize #41's third breaking-change bullet.
  - Nothing is implemented yet; this page is analysis only.
</Aside>
